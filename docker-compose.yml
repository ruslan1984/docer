version: '2'

services:
    nginx:
        image: nginx:latest
        ports:
            - "81:80"
        volumes:
            - ./hosts:/hosts
            - ./www/html:/www
            - ./logs:/var/log/nginx
